# JARVIS Backend Dependencies
# Python 3.12+
# Pin versions for production reproducibility

# ── Web Framework ──────────────────────────────────────────────
fastapi>=0.111.0,<1.0.0
uvicorn[standard]>=0.29.0,<1.0.0
pydantic>=2.7.0,<3.0.0
pydantic-settings>=2.3.0,<3.0.0

# ── Database ──────────────────────────────────────────────────
motor>=3.5.0,<4.0.0
pymongo>=4.7.0,<5.0.0
beanie>=1.26.0,<2.0.0  # Optional: ODM

# ── Authentication ────────────────────────────────────────────
python-jose[cryptography]>=3.3.0,<4.0.0
bcrypt>=4.1.0,<5.0.0
passlib>=1.7.4,<2.0.0

# ── AI / LLM (HTTP clients) ───────────────────────────────────
httpx>=0.27.0,<1.0.0

# ── Vector Memory ─────────────────────────────────────────────
numpy>=1.26.0,<2.0.0
# sentence-transformers>=3.0.0,<4.0.0  # Uncomment for local embeddings

# ── Search ────────────────────────────────────────────────────
# duckduckgo_search>=6.0.0,<7.0.0  # Optional: better DDG support

# ── Task Scheduling ───────────────────────────────────────────
croniter>=2.0.0,<3.0.0

# ── Monitoring ────────────────────────────────────────────────
psutil>=5.9.0,<6.0.0

# ── CORS ──────────────────────────────────────────────────────
# (handled by FastAPI)

# ── Development ───────────────────────────────────────────────
# pytest>=8.0.0,<9.0.0
# pytest-asyncio>=0.23.0,<1.0.0
# pytest-cov>=5.0.0,<6.0.0
# httpx>=0.27.0,<1.0.0
# aiomongo>=0.3.0,<1.0.0
